@*
    Minimal pass-through layout. The Home.razor page owns its full header
    and footer because the playground is single-page; introducing shared
    chrome would only add indirection for one consumer.
*@

@inherits LayoutComponentBase

@Body
